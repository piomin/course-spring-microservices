spring:
  application:
    name: discovery-server
  threads:
    virtual:
      enabled: true

server.port: 8761

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    evictionIntervalTimerInMs: 3000